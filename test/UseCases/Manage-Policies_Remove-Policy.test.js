var Promise = require('bluebird');

describe('Use Case Manage-Policies', function () {
  describe('Remove-Policy Scenario', function () {
    it('Remove-Policy Primary Test', function (done) {
       done();
    });
  });
});
