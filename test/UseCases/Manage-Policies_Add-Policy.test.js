var Promise = require('bluebird');

describe('Use Case Manage-Policies', function () {
  describe('Add-Policy Scenario', function () {
    it('Add-Policy Primary Test', function (done) {
       done();
    });
  });
});
