var Promise = require('bluebird');

describe('Use Case Manage-Policies', function () {
  describe('Add-Disable Scenario', function () {
    it('Add-Disable Primary Test', function (done) {
       done();
    });
  });
});
