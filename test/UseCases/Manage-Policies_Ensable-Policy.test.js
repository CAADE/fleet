var Promise = require('bluebird');

describe('Use Case Manage-Policies', function () {
  describe('Ensable-Policy Scenario', function () {
    it('Ensable-Policy Primary Test', function (done) {
       done();
    });
  });
});
