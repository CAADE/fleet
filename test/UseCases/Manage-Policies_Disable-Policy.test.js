var Promise = require('bluebird');

describe('Use Case Manage-Policies', function () {
  describe('Disable-Policy Scenario', function () {
    it('Disable-Policy Primary Test', function (done) {
       done();
    });
  });
});
